apiVersion: v1
kind: Service
metadata:
  name: user-order-management
  labels:
    app: user-order-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: user-order-management